#:schema https://developers.openai.com/codex/config-schema.json

# Project pin. A deliberate user or command-line override can still take precedence.
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "high"
